<UserControl x:Class="SettingsView.LogViewModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SettingsView"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Horizontal">
        <StackPanel Orientation="Vertical">
            <TextBlock TextAlignment="Center" Text="Type" />
            <ListBox IsSynchronizedWithCurrentItem="True" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" Margin="4" ItemsSource="{Binding}" DisplayMemberPath="Type" Width="133" Height="428" />
        </StackPanel>
        <StackPanel Orientation="Vertical">
            <TextBlock TextAlignment="Center" Text="Message" />
            <ListBox IsSynchronizedWithCurrentItem="True" Margin="4" ItemsSource="{Binding}" DisplayMemberPath="Message" Width="653" Height="428" />
        </StackPanel>
    </StackPanel>
</UserControl>
